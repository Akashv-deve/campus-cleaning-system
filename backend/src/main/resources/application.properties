spring.application.name=demo
# Render assigns a dynamic port. This defaults to 8080 for your local testing.
server.port=${PORT:8080}
# This hides your credentials by pulling from a secure Render environment variable.
spring.mongodb.uri=${MONGO_URI}