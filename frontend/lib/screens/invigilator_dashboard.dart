// lib/screens/invigilator_dashboard.dart
import 'package:flutter/material.dart';

class InvigilatorDashboard extends StatelessWidget {
  const InvigilatorDashboard({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Invigilator Dashboard')),
      body: const Center(child: Text('Invigilator features coming soon...')),
    );
  }
}